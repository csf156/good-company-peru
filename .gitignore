node_modules/
.expo/
.expo-shared/
dist/
web-build/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Env
.env
.env.local
.env.*.local

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Supabase
supabase/.branches
supabase/.temp

# subagent-driven-development scratch (briefs/reports/diffs, not source)
.superpowers/

# design-sync
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Claude Code local state (worktrees, permisos locales, locks).
# `launch.json` (config del preview) sí se versiona: sirve a cualquiera que
# levante la app, a diferencia del resto, que es de esta máquina.
.claude/*
!.claude/launch.json
