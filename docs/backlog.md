# Martini — Backlog (ideas fuera de alcance)

**Qué es esto:** aquí se **anota** cualquier cosa detectada durante una fase que pertenece a OTRA fase o que está fuera del alcance actual — en vez de hacerla. Mantiene la disciplina fase-por-fase (ver `../CLAUDE.md`).

**Cómo usarlo:**
- **Durante una fase:** si detectas algo útil pero fuera de alcance, añádelo abajo con una línea. No lo implementes.
- **Al planear una fase futura:** revisa si algo de aquí ya le corresponde; si se implementa, muévelo a la sección "Resuelto" con la fase que lo cubrió.
- Formato: `- [ ] <descripción> — detectado en fase <X.Y> — ¿pertenece a? <fase/área>`

---

## Pendiente

- [ ] **Sin tope en la cantidad de bebidas por propuesta — decisión del usuario del 2026-09-18.** BRAIN había recomendado un tope por importe total por antilavado. Queda registrado el riesgo: sin tope, **dos cuentas pueden mover importes grandes entre sí** con una sola propuesta aceptada; es la vía que vigila el veto 22. Hoy el límite práctico lo pone la autorización de la tarjeta, que fallará antes que la base. **Revisar antes de lanzar**, junto con la conversación sobre obligaciones de reporte con el abogado y con Red Pontis — detectado por BRAIN al diseñar la cantidad en la serie F.
- [ ] `lib/theme.ts` no tiene una escala de `lineHeight` — cada pantalla que necesita interlineado explícito (ej. `app/por-cobrar.tsx:135,154,166`) usa un literal (1.5×, etc.) en vez de un token. Bajo riesgo hoy, pero si crece el número de pantallas con esto, conviene una escala propia en el theme — detectado en E.4 Tarea 4 (pasada de diseño), 2026-09-17 — ¿pertenece a? mantenimiento de `lib/theme.ts` cuando haga falta de verdad.

- [ ] `hooks/useResendCooldown.ts` quedó sin ningún consumidor tras D.5: solo lo usaba `app/(auth)/verify-otp.tsx`, eliminada al sacar el OTP del login. El hook en sí es genérico y reutilizable (cooldown con segundos restantes), así que no se borró por si D.6 (segundo factor opcional por correo) lo necesita — pero si D.6 termina sin necesitarlo, hay que borrarlo junto con `tests/hooks/useResendCooldown.test.ts` — detectado en D.5, 2026-09-16 — ¿pertenece a? limpieza tras D.6, o antes si se confirma que no hace falta.

- [ ] **BLOQUEANTE DE LANZAMIENTO — el correo es ahora infraestructura crítica, y el mailer por defecto de Supabase no sirve para producción.** Al quitar el OTP como método de login (D.5), **el correo pasa a ser el único camino** para dos cosas: confirmar que una cuenta nueva es de quien dice ser, y **recuperar una contraseña olvidada**. Sin correo entregado, una persona no puede terminar de registrarse, y quien olvide su contraseña queda fuera para siempre. El mailer por defecto del plan gratuito tiene un límite de envío muy bajo: BUILDER lo alcanzó con **una sola prueba de registro** el 2026-09-16, y es la explicación más probable de que un correo real del usuario llegara con el enlace roto poco antes. Supabase lo documenta como no apto para producción. **Antes de lanzar: configurar SMTP propio** y revisar las plantillas de confirmación y recuperación, incluido que sus enlaces vuelvan a la app. Mientras tanto, cualquier verificación del flujo de registro choca con el mismo límite: probar varias veces seguidas no es posible — detectado por BUILDER en D.5 Tarea 1, registrado por BRAIN.
- [x] **Rediseño del login — RESUELTO en D.5 (2026-09-16).** Las únicas formas de entrar son correo+contraseña o Google; el OTP salió del login. Ver bitácora de D.5.
- [ ] **Segundo factor opcional (antes "D.6") — FUERA DE LA PRIMERA VERSIÓN, decisión del usuario del 2026-09-16.** El usuario había elegido **código por correo**. Queda registrado por qué no se construyó a la ligera, para cuando se retome: Supabase solo trae de fábrica **app autenticadora (TOTP) y SMS**. Un factor nativo sube la sesión de `aal1` a `aal2` y el servidor puede exigirlo con una política (`auth.jwt()->>'aal' = 'aal2'`). **Un código por correo a medida no puede subir la sesión**: el token de `aal1` ya es plenamente válido, y quien tenga la contraseña se salta la pantalla del código usando la API. Hecho a la ligera, parece un segundo factor y no protege nada. Construirlo bien exige una capa de verificación propia en el servidor que **cada política y cada función de dinero** tendría que comprobar. Si se retoma, **reconsiderar la app autenticadora**: es casi gratis con Supabase. Y **en esta app una cuenta robada retiene dinero en la tarjeta de su dueño**, así que vale la pena evaluar pedir el segundo factor al menos en los momentos de dinero (retener al invitar, aceptar una solicitud) aunque el login siga siendo libre.
- [ ] **Ningún test de este proyecto valida que una consulta del cliente case con el esquema real.** Todos los `tests/lib/*.test.ts` (y los de pantallas que leen datos) mockean `supabase`, así que prueban el **mapeo** de la respuesta — nunca la **consulta** en sí. Por eso `tests/lib/citas.test.ts` seguía en verde con `lib/citas.ts` pidiendo `invitaciones → bar → bebidas_catalogo` (`bar` eliminada en E.1) durante toda la Fase E: el mock nunca pudo objetar una cadena `select` que apunta a una relación que ya no existe. **Es la tercera vez en esta serie que un verde convive con un mundo que no existe**: las invariantes de conciliación comparando contra un estado que ninguna función produce ya (E.2a), un fixture con claves de idempotencia que la aplicación nunca genera (E.2a), y ahora esto (E.3). Dos salidas posibles, ninguna trivial: (a) un puñado de tests de integración reales (sin mock, contra la base de verdad, como el que se corrió a mano para verificar la Tarea 3b de E.3) que sirvan de canario para el resto de las consultas del cliente, o (b) generar tipos TypeScript desde el esquema real (`supabase gen types`) y que el cliente los use — un cambio de esquema que rompa una consulta se vería en `tsc`, no recién en producción. Lo que de verdad decide la urgencia: **hoy lo destapa un barrido manual; mañana lo destapa un usuario al que no le carga la cita** — detectado por BUILDER en E.3 Tarea 3 (escribiendo `lib/invitaciones.ts`), confirmado y barrido por BRAIN (es el único caso hoy: `.from('fotos')` es un bucket de storage, no una tabla), cubierto puntualmente en Tarea 3b — 2026-09-15.

- [ ] **BLOQUEANTE DE LANZAMIENTO — el `ledger` de producción arrancará con filas de prueba, y por diseño no se pueden borrar.** El trigger `ledger_append_only` prohíbe `DELETE` y `UPDATE` a **todos** los roles, `postgres` incluido — es la propiedad que sostiene el modelo contable y no debe tocarse. Consecuencia: toda verificación que llegue a la captura deja filas permanentes en el registro financiero. La primera ya está (E.2b Tarea 3: 3 filas de dos perfiles demo, netean a cero, cero discrepancias — verificado). Mientras se construye es inofensivo; **al lanzar, el libro contable no puede empezar con movimientos que nunca ocurrieron**. Además, `seed-demo.mjs` perdió en E.1 (Tareas 5/6) la lógica que sabía limpiar ledger de demo, así que hoy ni siquiera existe el mecanismo. Opciones, todas deliberadas y de una sola vez: (a) recrear la tabla antes de lanzar, (b) lanzar sobre un proyecto Supabase limpio y dejar éste como entorno de desarrollo —**probablemente lo correcto**, y hay que decidirlo antes de que haya un solo usuario real—, o (c) una migración de corte que marque el inicio contable. **Decidir antes del lanzamiento, no después** — detectado por BRAIN al verificar la Tarea 3 de E.2b, 2026-09-14.
  - **Actualización, E.2b Tarea 6 (2026-09-14) — la misma inmutabilidad tiene un segundo efecto, más amplio de lo que parecía.** `scripts/seed-demo.mjs` revivió la siembra de invitaciones/citas/chat sobre el flujo real; `--limpiar` necesitaba borrar `ordenes_pago` (no es append-only) antes del cascade desde `auth.users`, porque `ordenes_pago.perfil_id`/`.invitacion_id` son `'no action'`, no `'cascade'`. Una primera versión de ese borrado no distinguía el `estado` de la orden — y borrar una orden `'capturada'` deja sus 3 filas de `ledger` (que sí escribió, vía `capturar_orden`) **huérfanas para siempre**: `detectar_discrepancias_sp3()` las marca `ledger_sin_orden`, y su invariante agregada `escrow_captura_desbalance` (global, no por invitación) queda en rojo permanentemente. Se corrigió `limpiar()` para nunca borrar una orden `'capturada'` — pero eso implica, por la misma FK, que la invitación que la referencia **tampoco** se puede borrar, así que **ninguna de sus dos partes** (pague o no) es borrable, no solo quien pagó. En este repo esto ya dejó **13 discrepancias permanentes** en la base de dev (4 órdenes capturadas borradas antes del fix, de una sesión de verificación manual + de esta misma tarea) — `seed-demo.mjs` las tolera (solo aborta por discrepancias de la corrida actual, no por el total de la tabla; ver comentario en `seedInvitacionesCitasChat`). **No hay ruta de borrado en producción hoy** (nada llama a `capturar_orden` y luego borra la orden), así que no es explotable — pero si alguna vez se construye una herramienta de archivado/purga de órdenes viejas, tiene que respetar la misma regla: una orden `'capturada'` es, en la práctica, tan append-only como el `ledger` que escribió. Vincula a la opción (b)/(c) de arriba: cualquiera que sea la decisión de lanzamiento, tiene que asumir que "orden capturada" también sobrevive — detectado sembrando dos veces seguidas en E.2b Tarea 6. **Consecuencia de protección de datos, no solo técnica** (BRAIN, al revisar este hallazgo): "borrar mi cuenta" no puede ser un `delete` real para nadie que haya participado en un encuentro ya pagado — ni quien pagó ni la otra parte, por la misma FK. Cualquier futuro flujo de baja de cuenta tiene que anonimizar/desactivar, nunca intentar un hard-delete sobre un perfil con una invitación capturada detrás.
  - **Segundo addendum (E.2b, cierre, 2026-09-14): quedan 6 cuentas demo con contraseña real puesta, y algunas ya no se pueden borrar.** La verificación HTTP del despliegue necesitaba JWT de verdad, así que se les fijó contraseña temporal a 6 perfiles `demo=true` y se inició sesión real contra Supabase Auth. Son credenciales válidas sobre el proyecto en vivo. El riesgo hoy es bajo (correos `@martini.test`, proyecto pre-lanzamiento, ningún dato real), **pero se cruza con el hallazgo de arriba**: las cuentas demo que participaron en un encuentro **capturado** ya no son borrables ni por cascade, así que **esas contraseñas son permanentes en este proyecto**. No se limpian ahora a propósito: E.3 y E.4 van a necesitar la misma verificación por HTTP y rotarlas cada vez es churn sin ganancia. **Es una razón más, y de otra naturaleza, para la opción (b): lanzar sobre un proyecto Supabase limpio.** Si en algún momento se decidiera lanzar sobre ÉSTE, hay que rotar o invalidar esas 6 credenciales explícitamente, y no se puede resolver borrando las cuentas — detectado por BRAIN al verificar el despliegue de E.2b.
  - **Reset manual, 2026-09-14 (una sola vez, con autorización explícita del usuario, ejecutado por él mismo — nunca por el asistente, regla dura de esa sesión).** Las 13 discrepancias de arriba se limpiaron a propósito: `truncate public.ledger;` (no dispara `ledger_no_delete`/`ledger_no_update` — son triggers de fila, `TRUNCATE` no los alcanza; el trigger sigue intacto, nadie lo tocó) seguido de `delete from public.ordenes_pago;`, y después `npm run demo:limpiar` para el resto. Verificado antes de correrlo: los 2 perfiles reales (no demo) tenían **cero** filas de ledger — el reset no les tocó nada. Verificado después: `detectar_discrepancias_sp3()` en cero, los 2 perfiles reales siguen ahí, re-sembrado con `npm run demo:seed` y sigue en cero. **No es una herramienta ni un script — no quedó nada en el repo que lo repita.** Motivo: dejar limpia la señal de conciliación antes de E.3/E.4, que la necesitan de verdad. La entrada de arriba (BLOQUEANTE DE LANZAMIENTO) sigue vigente tal cual — este reset fue de la base de DESARROLLO, no cambia nada sobre qué hacer con la base de PRODUCCIÓN al lanzar.
- [ ] **Ninguna invariante de conciliación detecta una invitación encallada en `preautorizando`.** `detectar_discrepancias_sp3` cubre el hold huérfano (orden `preautorizada` cuya invitación ya se resolvió), pero no el caso inverso: una orden que se queda en `pendiente` para siempre porque el proveedor nunca respondió. Su invitación queda en `preautorizando` — **invisible para el receptor** por la lista blanca de la RLS (E.2a Tarea 2b) y sin nadie que la mueva. No hay dinero retenido (aún no hubo hold), así que la severidad es menor que la de un hold huérfano, pero es exactamente el mismo modo de fallo que el bug de la Tarea 3c: una fila viva que nadie ve y nada mueve. Cobra sentido en **E.2b**, cuando exista el Edge Function que llama al proveedor y pueda no responder. Invariante sugerida: orden `pendiente` con más de N minutos y su invitación en `preautorizando` — detectado por BRAIN al revisar la Tarea 5 de E.2a, 2026-09-14.
- [ ] **La conciliación atribuye ledger a órdenes con `idempotency_key like <orden_id>:%`, que no es indexable.** Es correcto y seguro (un UUID no contiene comodines `_` ni `%`, y el `:` impide que un id sea prefijo de otro), pero obliga a un escaneo por cada fila de ledger contra todas las órdenes. Irrelevante al volumen del MVP; degrada cuando el ledger crezca. Alternativa si llega a molestar: `split_part(idempotency_key, ':', 1)::uuid = o.id`, que sí es un join por igualdad — con cuidado, porque una clave malformada haría fallar el cast. **Causa raíz:** al mover `ledger.referencia_id` de la orden a la invitación (E.2a Tarea 2) se perdió el vínculo directo ledger→orden, y la clave de idempotencia quedó como único puente. Si alguna vez hace falta de verdad, la solución limpia es una columna `orden_id` en `ledger` — detectado por BRAIN al revisar la Tarea 5 de E.2a, 2026-09-14.
- [ ] **En E.2b, `responder_invitacion` tiene que pasar el `provider_ref` real al anular, no `null`.** Hoy (E.2a) llama `capturar_orden(v_orden.id, 'anulada', null)` — correcto en mock, donde no hay proveedor real todavía. Pero `mockAnular` (`_shared/pagos.ts`) devuelve un `providerRef` NUEVO (prefijado `anulado:`) que hay que persistir para que la guarda "capturar tras anular falla" se sostenga del lado del proveedor; `capturar_orden` ya soporta recibirlo (`coalesce(p_provider_ref, provider_ref)`). Si en E.2b se sigue pasando `null`, esa guarda se evapora en silencio y solo queda la de la base (`capturar_orden` rechazando una orden que no esté `preautorizada`) — detectado por BRAIN al revisar la Tarea 4 de E.2a, 2026-09-10 — ¿pertenece a? E.2b, al escribir el Edge Function que llama al proveedor real.
- [ ] **`select ... from bebidas_catalogo ... for update` serializa a todos los compradores de la misma bebida.** Lo hacen `crear_invitacion` y, desde E.2a Tarea 4, también `responder_invitacion`. El lock existe para que el precio no cambie a mitad de la transacción, pero bloquea la fila del catálogo: dos personas invitando con la misma bebida popular se ponen en cola una detrás de otra. Irrelevante al volumen del MVP, cuello de botella real si una bebida se vuelve la favorita. Alternativas: leer sin lock y congelar el precio en la orden (ya se congela), o `for share`. **No tocar sin medir** — detectado por BRAIN al revisar la Tarea 4 de E.2a, 2026-09-10 — ¿pertenece a? rendimiento, cuando haya volumen real.
- [ ] **Para el amigo, `preautorizando` y `pendiente` tienen que renderizarse IGUAL.** La RLS de `invitaciones` limita por estado al **receptor**, pero el **emisor ve su fila en todos los estados** (`emisor_id = auth.uid()`, sin filtro). En una `solicitud` el emisor es el amigo, así que ve su propia solicitud pasar por `preautorizando`. Y si la tarjeta del rentador falla, la ve volver a `pendiente` (E.2a Tarea 3b). Esa secuencia `pendiente → preautorizando → pendiente` **le revela que el rentador intentó pagar y su tarjeta fue rechazada** — información financiera de la otra parte que no le corresponde. No se arregla en la base: el emisor necesita ver su solicitud viva, y ocultarle estados propios crearía problemas peores. Se arregla en la UI: para el amigo, los dos estados dicen "esperando respuesta" y nada más, sin historial de transiciones ni etiqueta distinta. **Vincula a E.3/E.4** — detectado por BRAIN al revisar la Tarea 3b de E.2a, 2026-09-10.
- [ ] **El runner de pgTAP no valida el `plan(N)` declarado contra las aserciones ejecutadas.** `tests/db/run-pgtap.mjs:73` solo falla ante un `not ok`; pgTAP emite el desajuste plan-vs-ejecutadas como **comentario** (`# Looks like you planned N but ran M`), no como `not ok`, así que pasa invisible. Consecuencia: un archivo puede perder aserciones y seguir reportándose en verde — exactamente el modo de fallo del que depende la estrategia de invariantes en base de datos de la serie E. Caso vivo hoy: `15_invitaciones_rls.sql` declara `plan(9)` y ejecuta 8 (desajuste preexistente de la fase 4.0, no introducido por E.1). Arreglo: parsear la línea de plan (`1..N`) y fallar si N ≠ aserciones recogidas — detectado por BRAIN al revisar el cierre de E.1 — ¿pertenece a? higiene de tooling de testing, **antes de E.2** (que tiene que reponer 72 aserciones y necesita un runner en el que se pueda confiar para saber que las repuso).
- [ ] Redirect de `_layout.tsx` corre en `useEffect` post-render — pantalla protegida puede parpadear antes de redirigir. No explotable hoy, revisar cuando `app/index.tsx` tenga contenido sensible — detectado en fase 1.2 — ¿pertenece a? 1.6 o fase que añada contenido sensible a home.
- [ ] `perfiles_publicos` expone `kyc_estado` con granularidad completa (`pendiente`/`verificado`/`rechazado`) a cualquier autenticado; el badge de verificado solo necesita un booleano `verificado`. Sobre-disclosure leve, no explotable — detectado en auditoría 1.6 — ¿pertenece a? endurecimiento de vista pública (2.x visibilidad, o cuando se rediseñe el badge).
- [ ] El panel "Cita confirmada" del chat (`app/chats/[id].tsx`) muestra la hora en bruto (`2026-09-20T19:00:00+00:00`), sin formatear a algo legible ("20 sep, 7:00 pm"). Visto al verificar de punta a punta el cierre de E.3 (proponer → aceptar → abrir el chat), pero el panel en sí es de una fase anterior (4.5) — no se toca en E.3, que no lo modificó — detectado por BUILDER, 2026-09-15 — ¿pertenece a? la fase que retome el chat/panel de cita (5.x o 7.0, ver Gotchas de `CLAUDE.md` sobre el motor de cita de Lovable).
- [ ] `config.toml` no pinnea `verify_jwt = false` para `kyc-webhook`; depende del flag `--no-verify-jwt` en cada deploy (riesgo de drift). La seguridad NO depende de esto (el HMAC es la auth real), pero conviene fijarlo en config — detectado en auditoría 1.6 — ¿pertenece a? higiene de deploy / cuando se active Truora real (3.x+). **[Resuelto en 3.1: `[functions.*] verify_jwt` pinneado en config.toml para las 4 funciones. Mover a Resuelto al confirmar.]**
- [ ] `parseHoldWebhookPayload` (antes `parsePagoWebhookPayload`, renombrada en E.2b Tarea 1 junto con el vocabulario paid/failed → authorized/captured/voided/failed) solo mapea esos cuatro status; cualquier otro terminal real de Red Pontis (`refunded`, `disputed`, …) → null → webhook 400 → el partner puede reintentar indefinidamente. La estructura para un camino "ack, no-op" ya existe (`rpcParaEvento` devuelve `null` y `pago-webhook` lo ack-ea con 200 sin tocar la base) — falta el vocabulario real para poblarla, que no se conoce hasta activar Red Pontis — detectado en code-review 3.4, revisado en E.2b Tarea 4 — ¿pertenece a? cuando Red Pontis sea real.
- [ ] En `crear_invitacion`, el catch de `foreign_key_violation` asume que la FK que falla es siempre `bebida_bar_id` y responde "bebida no encontrada en tu bar" (AY409). Pero `receptor_id` también tiene FK a `profiles`: un `receptorId` inexistente dispararía la misma excepción y devolvería el mismo mensaje engañoso. Bajo riesgo (el receptor real siempre viene del descubrimiento, que ya filtra a perfiles existentes) — decisión explícita del usuario de no corregirlo en la fase 4.2. Distinguir con `GET STACKED DIAGNOSTICS` sobre el nombre de la constraint, o una validación de existencia explícita antes del insert — detectado en code-review de fase 4.2 — ¿pertenece a? robustez de `crear-invitacion`, retomar si se reporta un caso real o al tocar esa función de nuevo.
- [ ] **Sin infraestructura de push en todo el repo** (no `expo-notifications`, no tabla/columna de push token). El texto del plan menciona "notifica push a la contraparte" en 4.3, 4.5, 5.2, 5.3 sin que ninguna fase la construya explícitamente; 5.2 ("Cronómetro + notificaciones") es la candidata natural para sentar la costura real (registro de push token, envío vía Expo Push). `responder-invitacion` (4.3) dejó un `-- TODO(push)` de una línea en el SQL marcando dónde se conectaría, sin implementar nada — detectado en fase 4.3 — ¿pertenece a? 5.2, o una fase previa dedicada si el plan se ajusta.
- [ ] El runner casero de migraciones (`tests/db/apply-migrations.mjs`) deriva la "versión" de una migración del prefijo numérico antes del primer `_` del nombre de archivo. Si dos migraciones comparten el mismo prefijo (ya pasó una vez en este repo: dos archivos de la fase 4.2/hardening con el mismo timestamp), la segunda se trata como "ya aplicada" y se salta EN SILENCIO — sin error, sin aplicar su SQL. Es un footgun real de la herramienta, no del schema. Se resolvió ad-hoc renombrando el archivo colisionado (fase 4.3), pero el runner debería validar unicidad de prefijos (o fallar ruidosamente ante una colisión) en vez de confiar en que el autor no repita timestamp — detectado en fase 4.3 — ¿pertenece a? higiene de tooling de testing/DB, cualquier momento.
- [ ] En `responder_invitacion`, un tercero que llama con un `invitacionId` inexistente recibe `AY404`, pero uno que llama con un `invitacionId` real del que no es receptor recibe `AY403` — distinguible, revela existencia de la invitación a quien no es parte. Riesgo bajo (los ids son UUIDs no enumerables y la RLS ya expone la invitación solo a sus partes) — detectado en code-review de fase 4.3, aceptado tal cual — ¿pertenece a? endurecimiento de mensajes de error si se reporta abuso real.
- [ ] `comprar-bebida` en modo real: `partnerRes.json()` sin guardia — un 2xx con body no-JSON lanza tras crear la fila de orden, dejando una orden `pendiente` colgada (inofensiva, sin plata movida, la conciliación no la marca). Misma orfandad si el proceso muere entre el insert de la orden y la RPC en modo mock. Considerar limpieza de órdenes `pendiente` viejas — detectado en code-review 3.4 — ¿pertenece a? robustez / job de limpieza.
- [ ] Invariante #4 de la conciliación (conteo global `escrow_lock` vs `bar`) se vuelve frágil si se agrega borrado/anonimización de cuentas: `bar` tiene `on delete cascade` en `perfil_id` pero `ledger` no, así que borrar un perfil dejaría `escrow_lock` sin sus filas de `bar` → falso positivo permanente. Sin ruta de borrado hoy (no explotable) — detectado en code-review 3.4 — ¿pertenece a? la fase que introduzca borrado/anonimización de cuentas.
- [ ] `bar` con RLS que joinea `bebidas_catalogo` (filtrada a `activo=true`, Fase 3.0) pierde nombre/tipo/valor de una bebida ya comprada si el operador la desactiva del catálogo después — la UI de 3.3 lo tolera mostrando "Bebida no disponible" (no rompe), pero sería mejor que el dueño del stock siga viendo los datos de lo que ya pagó. Requeriría relajar la RLS del catálogo para filas referenciadas en `bar`/`ordenes_pago` propias, o congelar nombre/tipo/valor en `bar` al insertar — detectado en fase 3.3 — ¿pertenece a? endurecimiento 3.4.
- [ ] Discrepancia doc: el **plan** 3.1/3.2 y el resumen de fees en CLAUDE.md dicen que el rentador paga "buyer fee + procesamiento", pero el **diseño §A** (ejemplo trabajado) cobra solo V+15% y absorbe el ~4% de procesamiento del margen. Fase 3.1 implementó el **modelo del diseño §A** (decisión confirmada por el usuario). El procesamiento hoy NO se registra en ningún ledger (es margen/costo de plataforma) — cuando exista una cuenta contable de plataforma (conciliación 3.4 / niveles 6) habrá que registrarlo. Alinear el texto del plan/CLAUDE.md al diseño — detectado en fase 3.1 — ¿pertenece a? 3.4 (conciliación) o limpieza de docs.
- [ ] Worktree anidado `.claude/worktrees/fase-1-6-endurecimiento` (commit `dfca2da`, adelantado a master, `locked`) quedó de una sesión previa. Revisar si tiene trabajo sin mergear antes de borrarlo — detectado en fase 1.6.
- [ ] `getChats` (`lib/chat.ts`) hace una consulta `getPublicProfile` + una de último mensaje POR CADA cita (N+1). Volumen bajo en MVP, no es problema hoy — considerar batch/join cuando la lista de chats crezca — detectado en fase 4.4.
- [ ] El detector anti-fuga (trigger + su gemelo `_shared/moderacion.ts`) puede marcar un falso positivo con listas de números separadas por espacio que sumen 10+ dígitos compactados (p.ej. "20 30 40 50 60"). Decisión deliberada de la fase (priorizar cerrar la fuga sobre el caso límite raro) — no se corrigió — detectado en fase 4.4 — ¿pertenece a? ajuste del detector si se reporta un falso positivo real.
- [ ] `profiles.flags.chat_violaciones` es solo el contador (semilla para políticas futuras) — no hay ninguna acción de baja/suspensión/ban conectada todavía a ese contador. La regla de negocio dice "compartir contacto para pagar por fuera = ban" pero el ban en sí no existe — detectado en fase 4.4 — ¿pertenece a? sub-proyecto 7 (ratings/reportes/bloqueo, moderación, ToS enforcement).
- [ ] Regex de palabras clave anti-fuga: TS usa `\b(?:palabra)\w*` (con sufijo), SQL usa `\y(palabra)` (sin sufijo explícito) — el comportamiento resultante es equivalente (un match parcial de la palabra ya alcanza), pero la asimetría de escritura entre los dos "gemelos" podría confundir a quien los edite después sin notar que no hace falta igualar el sufijo. Solo el trigger SQL es la fuente de verdad en producción — detectado en code-review de fase 4.4, sin impacto de seguridad — ¿pertenece a? limpieza cosmética si se retoca el detector.
- [ ] El campo "hora" de "Confirmar cita" (fase 4.5) es texto libre validado con `Date.parse` cliente+servidor — no hay date-picker nativo en el repo y agregar una dependencia se consideró fuera de alcance para una fase "medium". Funciona pero la UX de capturar fecha/hora a mano es pobre — detectado en fase 4.5 — ¿pertenece a? pulido de UI cuando se porten componentes base, o cuando el diseño pida un picker nativo.
- [ ] `crear_invitacion` (fase 4.2) no valida que `emisor`/`receptor` tengan roles opuestos coherentes con `tipo` (`invitacion` debería requerir emisor=rentador/receptor=amigo; `solicitud` lo inverso) — solo exige `receptor_id != emisor_id`. En teoría dos rentadores (o dos amigos) podrían terminar en una `invitacion`/`solicitud` inconsistente si algo aguas arriba (descubrimiento 4.1) fallara en filtrar por rol opuesto. `confirmar_cita` (4.5) falla seguro ante esto (nadie matchea `rol='amigo'` → `AY403` para cualquiera, nunca la persona equivocada), así que no es explotable hoy, pero la causa raíz sigue sin validarse en el punto de creación — detectado en code-review de fase 4.5 — ¿pertenece a? endurecimiento de `crear_invitacion` si se reporta un caso real, o revisión final del sub-proyecto 4 (fase 4.6).

- [ ] **Auditar las integraciones externas de las fases ya cerradas ✅.** La fase 1.2 (auth) se cerró verde con OTP por celular implementado y testeado, pero el proveedor de SMS **nunca se habilitó** en el proyecto Supabase (`external_phone_enabled: false`, `sms_twilio_account_sid: null`, verificado 2026-07-24 vía Management API). Los tests mockean el cliente de Supabase, así que pasaban igual. El mismo patrón puede afectar a **KYC (Truora, fase 1.4)** y **pagos (Red Pontis, fase 3.1)** — ambos corren hoy en modo mock. Antes de confiar en el estado ✅ de esas fases, verificar contra la configuración real del proveedor, no contra los tests — detectado 2026-07-24 al preparar el login con Google — ¿pertenece a? auditoría de integraciones, antes de cualquier intento de producción.
- [ ] **Verificación de celular por SMS.** Descartada del spec de design system (2026-07-24) por costo: no existe opción gratuita para producción (`sms_test_otp` solo sirve para demo; los trials de Twilio/Vonage solo envían a números verificados uno por uno). Si el producto llega a necesitar celular verificado — KYC real, encuentros presenciales, anti-fraude — requiere proveedor de pago (~S/0.20 por SMS) y una fase propia — ¿pertenece a? sub-proyecto 7 (confianza/seguridad) o cuando se prepare producción.
- [ ] **Pantalla de estado KYC** (`pendiente` en revisión / `rechazado` con motivo y reintento). Propuesta como 🆕 en `docs/2026-07-03-design-system.md:118`; **ningún sub-proyecto de ninguno de los dos planes la toma**. Sin ella el usuario queda colgado tras subir su DNI — detectado 2026-07-24 al verificar choque de alcance del spec de design system — ¿pertenece a? sub-proyecto 1 (identidad) si se reabre, o la fase que retome KYC real con Truora.
- [ ] `@types/node` no es una dependencia directa en `package.json`, solo existe transitivamente en `node_modules` (arrastrada por otra dependencia) — funciona hoy pero es frágil. Sugerido agregarla como devDependency explícita — detectado en revisión de la Tarea 12 (guardia anti-color-literal) de la fase de design system, 2026-07-25 — ¿pertenece a? tarea de mantenimiento futura de dependencias.
- [ ] Expiración de invitación no implementada → bebida bloqueada huérfana. El valor `expirada` del enum `estado_invitacion` no se setea en ningún camino; una `invitacion` `pendiente` nunca respondida deja su bebida `bloqueada` indefinidamente (`crear_invitacion` la bloquea, solo `rechazar` la libera). No explotable (es la propia bebida/escrow del rentador, auto-infligido, sin pérdida de fondos ni cruce de usuarios) y sin infraestructura de cron/expiración en el MVP — detectado en security-review de fase 4.6 — ¿pertenece a? 5.x (motor de cita: no-show/expiración + cron), o una fase de barrido de invitaciones vencidas.
- [ ] Sin punto de entrada cliente para crear/responder invitaciones: las Edge Functions + SQL `crear-invitacion` y `responder-invitacion` (4.2/4.3) están completas y testeadas, pero ningún componente las invoca — el CTA de descubrimiento (`app/index.tsx`) y "Invitar" de `bar.tsx` son inertes (sin `onPress`), y no existe pantalla de invitaciones entrantes (aceptar/rechazar). Como la `cita` nace solo al aceptar, el chat (4.4) y confirmar (4.5) —que sí están cableados— no se alcanzan end-to-end desde la app. El cableado del CTA se difirió deliberadamente en 4.1 pero ninguna fase posterior lo retomó — detectado en code-review de fase 4.6 — ¿pertenece a? una fase de cableado UI de invitaciones (panel `invitations.tsx` del mapeo Lovable); decisión de alcance pendiente con el usuario.
- [ ] `hora` de confirmar cita (4.5) es texto libre sin offset (p.ej. "2026-07-25 21:30") y se castea a `timestamptz` en `confirmar_cita` bajo la sesión Postgres en UTC → el instante se guarda como 21:30Z (16:30 Lima), 5h corrido respecto a la intención local. Hoy sin impacto visible (`app/chats/[id].tsx:147` muestra el string crudo, ambas partes ven el mismo literal) pero el instante quedará mal para cualquier consumidor que compute contra `hora` (cronómetro / no-show ~30 min post-hora de 5.x). Ligado a la deuda ya anotada de "hora como texto libre sin date-picker"; la corrección natural va con el picker + offset fijo de Perú (UTC-05:00) — detectado en code-review de fase 4.6 — ¿pertenece a? 5.x o el pulido de UI que introduzca el date-picker.
- [ ] `jest.config.js` no acota `maxWorkers` (default: `numCPUs - 1`, 11 workers en una máquina de 12 CPUs). Con cache de transform fría y suite completa en paralelo, varias suites (`chat-detail`, `profile-setup`, `store`, `sign-in`) rozan o superan el `testTimeout` global de 15s por contención de CPU — `chat-detail` llegó a fallar, las otras quedaron al límite (24-26s de suite). El fix aplicado en Fase 0 solo subió el timeout de `chat-detail` (la que reprodujo el fallo real); las demás quedan expuestas al mismo mecanismo. La causa raíz es la sobre-suscripción de workers, no el timeout — considerar `maxWorkers: '50%'` en `jest.config.js` en vez de subir timeouts suite por suite — detectado en Fase 0 (diagnóstico del flake de `chat-detail`, 2026-08-25) — ¿pertenece a? mantenimiento de tooling de tests.
- [ ] El pooler de Supabase (Supavisor) para el proyecto `zmtclismywmkratmufov` estuvo `UNHEALTHY` (`{"error":"not found"}` en ambos puertos, sesión y transacción) el 2026-08-25, aunque el proyecto en sí reportaba `ACTIVE_HEALTHY` — confirmado vía Management API (`/health?services=pooler`). `DATABASE_URL` en `.env` se cambió temporalmente al host directo (`db.zmtclismywmkratmufov.supabase.co:5432`, usuario `postgres`, misma password) porque ese host sí respondía — resuelve solo por IPv6, funciona en esta máquina porque tiene salida IPv6. Si algún día se corre `npm run test:db` desde una red sin IPv6, hay que volver al pooler y antes verificar su health con la Management API — detectado en Fase 0 (Tarea 3) — ¿pertenece a? nadie por ahora; es una nota operativa, no un bug del repo.
- [ ] **El desglose de fees (buyer fee 15%) vive en dos sitios: TypeScript (`calcularDesgloseCompra`, `_shared/pagos.ts`) y ahora también SQL (`crear_invitacion`, réplica en centavos para coincidir al céntimo).** Es deriva conocida y aceptada por ahora — `crear_invitacion` necesita el monto server-side dentro de la misma transacción que crea la invitación, no puede llamar a Deno. Cuando los niveles (sub-proyecto 6) hagan el fee variable por nivel del rentador, hay que unificar en una sola fuente de verdad (ej. una función SQL que ambos lados consulten, o mover el cálculo completo a SQL) — detectado en Fase E.2a (Tarea 3) — ¿pertenece a? sub-proyecto 6 (niveles) al tocar fees variables.
- [ ] **La atomicidad de `responder_invitacion` capturando el hold se rompe en E.2b.** En E.2a, `capturar_orden` es una función SQL que corre *dentro* de la misma transacción que `responder_invitacion` — si lanza, aborta todo (atómico de verdad). En E.2b, cuando la captura real sea una llamada HTTP al proveedor (Red Pontis), esa propiedad deja de existir: un `fetch` no participa de una transacción de Postgres. El orden correcto entonces es capturar en el proveedor PRIMERO y escribir la base después, con la idempotencia de `capturar_orden` (CAS de estado) como red ante un fallo entre medias — no resuelto en E.2a a propósito — detectado en Fase E.2a (Tarea 4, nota de BRAIN en el plan) — ¿pertenece a? E.2b, al escribir el Edge Function `responder-invitacion` real.

- [ ] **Repo `rent-a-friend-peru` (GitHub, mismo dueño) es una copia vieja abandonada** de este proyecto — último push 2026-07-05, sin `app.config.js`, sin fase D.2, sin docs actuales. El proyecto real vive en la rama `master` de `good-company-peru` (la rama `main` de ese mismo repo es el prototipo Lovable). Decisión del usuario (2026-08-25): dejar la estructura como está por ahora, no migrar. Pendiente decidir algún día: archivar/borrar `rent-a-friend-peru`, o resucitarlo como home propio del proyecto — detectado 2026-08-25 al pedir el link de GitHub Pages — ¿pertenece a? limpieza de infraestructura, sin fase asignada.
- [ ] `isMayorDeEdad` (`lib/validation.ts`) calcula el 18° cumpleaños con `new Date(nacimiento.getFullYear() + 18, nacimiento.getMonth(), nacimiento.getDate())`. Para alguien nacido el 29 de febrero, ese cálculo cae el 1 de marzo en un año no bisiesto (JS normaliza el día 29 inexistente al día siguiente), así que esa persona pasa a ser mayor de edad un día después del corte estricto. El error va hacia el lado seguro (nunca deja pasar a un menor) y el caso es marginal — detectado en fase D.3 bloque 1 (revisión, 2026-08-25) — ¿pertenece a? pulido de `isMayorDeEdad` si se reporta un caso real.
- [ ] El pooler de Supabase volvió a fallar el 2026-08-25 (mismo día que el incidente ya anotado arriba), esta vez al revés: el proyecto completo quedó `UNHEALTHY` (`db`/`auth`/`rest`/`storage`, causa raíz `password authentication failed for user "supabase_admin"`) y el host directo, que había servido de respaldo esa mañana, **dejó de resolver por completo** (ni `dns.lookup` de Node ni consultas posteriores lo encontraron — se acabó la salida IPv6 utilizable en la máquina). El usuario reinició el proyecto desde el dashboard y `DATABASE_URL` volvió al pooler (puerto 5432, modo sesión) con las credenciales `postgres.zmtclismywmkratmufov`. Queda registrado que el camino directo por IPv6 **no es confiable en esta máquina** — no usarlo como plan de respaldo por defecto; si el pooler falla de nuevo, verificar su health por la Management API antes de intentar nada — detectado en fase D.3 bloque 1 (Tarea 2, 2026-08-25) — ¿pertenece a? nadie por ahora; nota operativa.
- [ ] `kyc-webhook` y `pago-webhook` quedaron deliberadamente sin CORS al arreglar el resto de las Edge Functions invocables desde el cliente (2026-09-04). Los llama un partner (Truora / Red Pontis) servidor a servidor, donde CORS no existe — agregárselo sería ruido y ampliaría superficie sin ganancia. Si algún día alguna de las dos se invoca desde un navegador, necesita el mismo tratamiento que las otras cinco — detectado al escribir el plan de CORS — ¿pertenece a? nadie por ahora, es la decisión de alcance ya tomada, no un olvido.
- [ ] Al desplegar el fix de CORS (2026-09-04), la Management API mostró `comprar-bebida` y `confirmar-cita` en **`version: 1`** — su primer despliegue real a la nube, pese a que sus fases (3.4 y 4.5) llevan semanas cerradas ✅. `kyc-start`, que sí se había desplegado antes, mostró `version: 4`. Mismo patrón que ya dejó el proveedor de SMS de la fase 1.2 sin habilitar pese a tests en verde (nota en este mismo archivo, sección Resuelto/Pendiente de auditoría) — una fase se cierra cuando el código y los tests están verdes, no cuando lo que depende de infraestructura externa (deploy, proveedor configurado) se confirmó en la realidad. Antes de confiar en el estado ✅ de cualquier fase que incluya una Edge Function, verificar por la Management API que está desplegada, no solo que el código existe — detectado en fase de arreglo de CORS (2026-09-04) — ¿pertenece a? auditoría de integraciones / infraestructura, mismo dueño que la nota de SMS/Truora/Red Pontis ya anotada.
- [ ] **`git log` está torcido en `e699987`:** ese commit dice `feat: siembra y limpieza de datos de demo` pero además de la siembra contiene `docs/superpowers/plans/2026-09-04-kyc-dos-pantallas.md` (987 líneas), que no es suyo y su mensaje no menciona. Causa: dos sesiones (BRAIN y BUILDER) commiteando en el mismo working tree; un `git commit --amend` de BUILDER cayó sobre el commit de BRAIN (`003a664`), que era HEAD en ese instante. **Nada se perdió** — el árbol es correcto y el mensaje original sigue recuperable con `git log --format=%B -1 003a664`. Se decidió NO reescribir el historial (2026-09-04, decisión explícita del usuario): cuando se detectó, `e699987` ya no era el tip y el arreglo pasaba de un `--amend` a reescribir dos commits de trabajo que toca dinero. Consecuencia práctica: `git log` de ese archivo de plan apunta a un commit de siembra. Protocolo acordado para evitar la repetición: nadie usa `--amend` ni `git add -A` mientras dos sesiones compartan working tree, y cada sesión tiene su carril de archivos — detectado en fase de datos-demo (2026-09-04) — ¿pertenece a? nada, es histórico; anotado solo para que no confunda a quien audite `git log` más adelante.
- [ ] **La extensión `pgtap` está instalada en el esquema `public` de la base de PRODUCCIÓN.** Sus vistas (`tap_funky`, `pg_all_foreign_keys`) son propiedad de `supabase_admin` y conservan `TRUNCATE` para `anon`/`authenticated` — el único residuo que la migración `20260906120000` no pudo cerrar, porque `postgres` no es superusuario acá y no puede revocar sobre objetos ajenos (ver la entrada correspondiente en Resuelto). Una extensión de testing no debería vivir en el esquema que sirve la API: lo correcto sería instalarla en un esquema propio (`extensions`, o uno `tap` dedicado) o no tenerla en producción. Requiere `supabase_admin`, así que no se puede hacer desde las migraciones de este repo — es trabajo de configuración del proyecto Supabase, no de código. Riesgo hoy: bajo (son vistas de introspección, PostgREST no las expone, y truncarlas no tocaría datos de la app) — detectado al revocar TRUNCATE (2026-09-06) — ¿pertenece a? higiene de infraestructura / preparación de producción.
- [ ] **BLOQUEANTE DE LANZAMIENTO: el libro de reclamaciones virtual no existe, y el ToS afirma que sí.** La cláusula 22 del articulado dice que el Operador "habilita un libro de reclamaciones virtual accesible desde dichos medios". Hoy no hay ninguno: `git grep -i reclamacion` sobre `app/`, `lib/` y `public/` no devuelve nada. En Perú el libro de reclamaciones es obligatorio para proveedores de consumo y su ausencia es infracción sancionable por sí sola, así que borrar la frase tampoco resuelve — hay que construirlo. Decisión del usuario (2026-09-07): la cláusula se queda, y tener el enlace en línea es requisito de lanzamiento. Detectado por la sesión de revisión legal al redactar el ToS — ¿pertenece a? preparación de lanzamiento, antes de cobrar a cualquier usuario real.
- [ ] **CRUCIAL ANTES DE LANZAR: el ToS describe mecanismos que la app todavía no tiene.** La cláusula 11 detalla la verificación del encuentro —"la lectura de un código en el lugar del Encuentro, la comprobación de proximidad geográfica y el registro de su duración"— y la cláusula 15 remite a "las reglas de cancelación, inasistencia y disputas vigentes publicadas en la aplicación". Ninguna de las dos cosas existe: el motor de cita es el sub-proyecto 5 (fases 5.0 a 5.5, todas ⬜ en la tabla de este mismo repo) y las reglas de cancelación son parte de 5.4. Un contrato que describe un mecanismo que el producto no puede ejecutar es inejecutable en el mejor caso y engañoso en el peor. Para un borrador interno es tolerable; antes de que un usuario real acepte y pague, no. Decisión del usuario (2026-09-07): se anota como tema crucial a resolver antes del lanzamiento. Detectado en la revisión del ToS (BRAIN) — ¿pertenece a? sub-proyecto 5, y revisión del ToS una vez construido.
- [ ] **Puntos que debe revisar un abogado colegiado en el Perú antes de publicar el ToS.** Levantados por la sesión de revisión legal (2026-09-07), ninguno resoluble desde el código: (a) exposición penal del operador frente a los tipos de favorecimiento a la prostitución, rufianismo y proxenetismo, y frente a la ley de trata de personas — la conclusión de esa revisión es que la prohibición contractual y la moderación NO bastan, y hace falta un protocolo escrito de detección, bloqueo, conservación de evidencia y comunicación a la autoridad, que además es lo que se le enseña al procesador de pagos cuando pregunte; (b) si la bebida virtual califica como dinero electrónico y exige licencia ante la SBS (ver la entrada siguiente); (c) protección de datos: biometría y DNI son datos sensibles, hay geolocalización en tiempo real y encargados de tratamiento en el extranjero, lo que activa flujo transfronterizo y probablemente DPO y evaluación de impacto — la política de privacidad actual es demasiado corta para ese perfil de riesgo; (d) control de cláusulas abusivas sobre la limitación de responsabilidad y sobre la sumisión a tribunales de Lima, que puede leerse como abusiva frente a un consumidor de provincia; (e) riesgo de reclasificación laboral del amigo en renta pese a la cláusula de no-laboralidad, si la app fija precio, asigna encuentros, califica y sanciona; (f) tratamiento tributario y obligación o no de retener; (g) el ToS dice "su titular" porque no hay razón social: antes de publicar hacen falta denominación social, RUC y domicilio, cuya ausencia es bandera roja para el procesador y para INDECOPI — ¿pertenece a? revisión legal externa, antes del lanzamiento.
- [ ] **Imágenes y descripción por bebida en el catálogo.** El usuario, recorriendo el preview de E.3, pidió que cada bebida en el picker de "Elige una bebida" muestre una imagen y una frase corta de para qué sirve la salida. `bebidas_catalogo` hoy solo tiene `id, nombre, tipo_invitacion, valor_v, activo` — así se definió desde el plan MVP (3.1); nunca hubo campo de imagen ni de descripción en ningún doc. Requiere columna(s) nueva(s), Storage para las imágenes, y contenido real escrito para las 5 bebidas — trabajo de contenido/catálogo, no un ajuste de UI de una fase que ya está construyendo sobre datos existentes — detectado por el usuario en el recorrido de cierre de E.3, 2026-09-15 — ¿pertenece a? una fase de catálogo/contenido, por decidir.
- [ ] **"Objetivo de la salida" (solo compañía / evento social / salida casual…) es un campo que no existe — y `tipo_invitacion` de `bebidas_catalogo` NO sirve para llenarlo.** Mismo pedido del usuario que la entrada de arriba, pero es un campo de producto distinto, no una mejora de UI: dos razones independientes, cualquiera basta (BRAIN, revisando el pedido). (1) `tipo_invitacion` describe la BEBIDA, no la SALIDA — que un Pisco Sour sea `'divertida'` no dice nada del encuentro que se está proponiendo, y etiquetarlo así invitaría a leerlo como si lo dijera. (2) El enum de `tipo_invitacion` incluye `'romantica'`, categoría que el usuario decidió explícitamente excluir del producto (`docs/superpowers/specs/2026-08-25-onboarding-premium-design.md` §7: "romántico" corre la lectura de compañía social a citas pagadas, el encuadre por el que un procesador cierra una cuenta). Hoy ninguna bebida activa usa `'romantica'`, pero el valor sigue vivo en el enum — exponer `tipo_invitacion` en UI deja la etiqueta lista para aparecer el día que alguien dé de alta una bebida así, sin que nadie vuelva a tomar esa decisión. El "objetivo de la salida" real —si se construye— es un campo nuevo por diseñar: ¿vive en la invitación, en el perfil (donde ya existe `tipo_salida`, con la misma exclusión de "romántica" ya resuelta ahí), o como nota libre (que §7 también descarta para `tipo_salida`, por la misma razón: reabre la puerta de atrás)? Es diseño de producto, no ajuste de pantalla — detectado por el usuario en el recorrido de cierre de E.3, lectura de BRAIN, 2026-09-15 — ¿pertenece a? diseño de producto, por decidir con el usuario.


---

## Funcionalidades vetadas — no proponer, no construir

**Qué es esto:** la lista de cosas que Martini NO puede hacer nunca sin cambiar de naturaleza regulatoria. No son decisiones de producto reversibles: cada una, por sí sola, convierte la bebida virtual en dinero electrónico o la cuenta por cobrar del amigo en un monedero, y con eso aparece la licencia ante la SBS.

**De dónde sale:** análisis de la sesión de revisión legal (2026-09-07) sobre la Ley 29985 y su reglamento (DS 090-2013-EF), tras las tres decisiones estructurales del usuario: compra al invitar, saldo del amigo como cuenta por cobrar, y titularidad de la custodia en la pasarela. La defensa principal es que el reglamento excluye del concepto de dinero electrónico los soportes "diseñados para atender usos generales y no aquellos para usos específicos" — verificado contra el texto de la norma. Todo lo que amplíe el universo de usos de la bebida debilita esa defensa.

**Lo que revive el valor almacenado del lado comprador**
1. Recargar saldo o comprar crédito sin destinatario.
2. Comprar bebidas para stock en cualquier forma: packs, promociones "lleva 3", bebidas de cortesía acumulables.
3. Reasignar una bebida ya comprada a otro amigo o a otro encuentro tras un rechazo o cancelación.
4. Devolver al crédito interno en lugar del medio de pago original.
5. Bebidas con fecha de vencimiento. Si vencen es porque se almacenan — y además el vencimiento de valor prepagado es problema de protección al consumidor por separado.
6. Códigos, vouchers o gift cards de bebidas canjeables por quien los tenga.
7. Que un tercero pague la bebida por el rentador.

**Lo que convierte la cuenta por cobrar en monedero**
8. Regalar o transferir bebidas o importes entre usuarios.
9. **Que el amigo use lo que ganó para invitar a alguien.** El más peligroso de la lista, porque es el que más se va a pedir y el que suena más inocente. Si un amigo quiere ser rentador, cobra a su banco y vuelve a entrar pagando con tarjeta como cualquiera.
10. Propinas pagadas desde el importe pendiente de liquidación.
11. Pagar la suscripción premium con el importe pendiente. Suena razonable y compensable, y es exactamente efecto cancelatorio dentro de la plataforma.
12. Liquidar a cuenta bancaria de un tercero, o a cuenta cuya titularidad no coincida con el KYC.
13. Retiro en efectivo por agente o corresponsal.
14. Interés, rendimiento, "gana por dejar tu plata", o cashback acreditado como importe liquidable.
15. Créditos promocionales o de referidos que caigan en el mismo bucket liquidable. Van en bucket separado no convertible, o son descuento sobre el precio. **Afecta al sub-proyecto 9.**
16. Que el usuario pueda elegir acumular, o umbral mínimo que en la práctica lo obligue a acumular. **Afecta al diseño de la fase 6.3.**
17. Retener el importe pendiente tras la baja de la cuenta. La baja no extingue la deuda: se liquida.

**Naming y presentación — no es cosmético**
18. Prohibido llamarlo "saldo", "billetera", "monedero" o "wallet" en la UI, en el ToS, en soporte y en marketing. Es "pagos pendientes de liquidación" o "por cobrar". El procesador y el regulador leen la interfaz, no el diagrama de arquitectura.
19. Prohibido mostrar el importe pendiente como si fuera poder de compra dentro de la app.
20. Prohibido expresar precios en bebidas con tipo de cambio propio. La bebida siempre muestra su importe en soles, 1:1.

**Lo que perfora el control antilavado**
21. Liberación manual por soporte sin verificación presencial, salvo con doble control humano, causal tipificada y registro auditable. Nunca un botón de un clic para un agente.
22. Comprar y cancelar en ciclo sin límite. Es una ruta de cash-out.

**Exigencia de ingeniería, no de disciplina.** Nada de esto se sostiene como regla de aplicación: dentro de dos fases alguien escribe un endpoint de crédito "solo para pruebas" o "solo para compensar a un usuario molesto" y ahí muere el argumento. Las reglas 1, 8, 9, 12 y 14 deben ser **invariantes de base de datos**: el `ledger` solo admite créditos a favor de un amigo con origen en una liberación de escrow referida a un encuentro verificado, y débitos solo con destino en una liquidación bancaria a titular verificado. Que un `INSERT` que viole eso falle en Postgres, no en TypeScript.

**Y una que es peor que todas juntas:** si alguna vez se remunera el importe retenido (intereses, rendimiento), el problema deja de ser la Ley 29985 y pasa a ser captación de fondos del público sin autorización — materia de la Ley 26702 y del Código Penal. Estrictamente peor.

---

## Resuelto

- [x] **`TRUNCATE` concedido a `anon`/`authenticated` en todo `public`** (detectado al cerrar el Bloque 3 de D.3) — **cubierto por la migración `20260906120000_revoke_truncate.sql`:** `revoke truncate on all tables in schema public` (presente) + `alter default privileges for role postgres ... revoke truncate` (futuro). Verificado por introspección: los 28 grants sobre relaciones propias del proyecto pasaron a 0, y una tabla nueva creada por `postgres` ya no lo hereda. pgTAP `26_revoke_truncate_default.sql`, 6 aserciones. **Dos residuos que NO se pueden cerrar desde este proyecto, documentados a propósito:** (a) las vistas `tap_funky` y `pg_all_foreign_keys` (extensión pgtap, propiedad de `supabase_admin`) conservan el privilegio — `postgres` no es superusuario acá (`rolsuper=false`) y no puede revocar sobre objetos ajenos; el pgTAP las fija por nombre para que cualquier OTRA relación que recupere TRUNCATE haga fallar el test. (b) El default privilege del rol `supabase_admin` sigue con la `D` (`anon=arwdDxtm/supabase_admin`), así que una tabla creada POR ESE ROL sí heredaría TRUNCATE; `alter default privileges for role supabase_admin` devuelve `permission denied`. El camino real (migraciones de este repo y editor SQL de Supabase, ambos como `postgres`) sí queda cubierto — comprobado creando una tabla de prueba en una transacción revertida.
- [x] Path traversal en rutas de storage de `kyc-start` (`startsWith` no bastaba) — cubierto en fase 1.6 (fix ya escrito, pendiente commit).
- [x] `verifyWebhookSignature` aceptaba firma HMAC con secreto vacío/`undefined` — cubierto en fase 1.6 (fix ya escrito, pendiente commit).
- [x] Creación de orden en `comprar-bebida` no idempotente por-click (detectado 3.1, confirmado en code-review 3.4) — **cubierto en fase 3.4:** `ordenes_pago.idempotency_key` unique (migración `20260723170000`); `newIdempotencyKey` (`lib/tienda.ts`) genera una key por intento reusada en reintentos (`store.tsx` la conserva ante error, la descarta tras éxito); `comprar-bebida` inserta con la key y ante `23505` devuelve la orden existente sin crear otra.
- [x] `pago-webhook` devolvía 404 ante CUALQUIER error de la RPC, no solo "orden no encontrada" — un error transitorio de DB también daba 404, que le dice al partner que deje de reintentar → se podía perder una confirmación reintentable (detectado en code-review 3.4) — **cubierto en E.2b Tarea 4** (distinguía por el texto del mensaje, único diferenciador disponible entonces) **y endurecido en Tarea 4b:** `capturar_orden`/`confirmar_preautorizacion` no tenían errcode propio en su `raise exception 'orden no encontrada: %'` — un simple retoque de redacción del mensaje lo habría roto en silencio (BRAIN, code-review de la Tarea 4). Migración `20260914120000_ay404_orden_no_encontrada.sql` les agrega `AY404` (mismo vocabulario AY4xx que crear_invitacion/responder_invitacion/confirmar_cita); `pago-webhook` pasa a mapear por `error.code` con el mismo patrón `ERRCODE_STATUS` que usan crear-invitacion/responder-invitacion, en vez de comparar el string del mensaje. `pago-webhook/index.ts` reescrita entera junto con el rename de vocabulario (paid/failed → authorized/captured/voided/failed, Tarea 1).
- [x] Conciliación solo verificaba forma (netea a 0), no montos — **cubierto en fase 3.4:** 5ª invariante `ledger_monto_no_coincide_con_orden` en `detectar_discrepancias_sp3` (migración `20260723160000`) compara los montos del ledger contra los congelados en `ordenes_pago`.
- [x] `ordenes_pago.idempotency_key` era único GLOBAL, no por-emisor (migración `20260723170000`, fase 3.4): ante colisión (`23505`), `comprar-bebida` devolvía la orden existente sin verificar `perfil_id` — un usuario B que reusara la key de A recibía la orden de A. Detectado en code-review de fase 4.2 (por comparación con el mismo bug en `invitaciones`) — **cubierto en SP3 hardening puntual (2026-07-24, fuera de la secuencia 4.x, autorizado explícitamente):** migración `20260724140000_orden_idempotency_scope.sql` reescopa el índice a `(perfil_id, idempotency_key)`; `comprar-bebida` filtra el recovery `select` por `perfil_id` además de la key. pgTAP prueba que mismo-perfil+misma-key sigue siendo idempotente y que distinto-perfil+misma-key ya no colisiona. Commit `609b4b5`, revisado (security-review, Opus 4.8) y aprobado.
- [x] Portar componentes base `level-badge`, `drink-icon`, `friend-card`, `app-shell` de Lovable — detectado al reconstruir bitácora 1.0 (2026-07-22) — **cubierto parcialmente en fase D.1 (2026-07-25):** el design system quedó unificado bajo tokens Martini (`lib/theme.ts`) y las 13 pantallas migradas usan `Button` + `Screen` como únicos componentes base. `level-badge`, `friend-card`, `drink-icon` y `app-shell` **siguen fuera** — se portan recién cuando la fase que primero los necesite los reclame (candidatas: paneles de nivel para `level-badge`, discovery/swipe para `friend-card`, tienda/bar para `drink-icon`, cualquier pantalla con tab bar para `app-shell`).
- [x] `<Screen framed>` (ancho máximo centrado, bloque 2b de D.3) solo se había activado en `profile-setup.tsx` — detectado en fase D.3 bloque 2b (2026-09-01). **Cubierto el mismo bloque (extensión, 2026-09-04):** el usuario pidió el ancho máximo para toda la app, no solo el alta. Se invirtió el default de `Screen` (`framed=true`) en vez de agregar la prop pantalla por pantalla; las 13 pantallas que usan `Screen` lo heredan sin tocarse. La textura de puntos sí se quedó exclusiva de `profile-setup` vía una prop `textured` separada — eso no lo pidió el usuario, extenderla sería decisión visual, no un pedido suyo.

